% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval-key-map.R
\name{find_automorphism_index}
\alias{find_automorphism_index}
\title{Compute the automorphism index for a single slot index}
\usage{
find_automorphism_index(cc, index)
}
\arguments{
\item{cc}{A \code{CryptoContext}.}

\item{index}{Integer; the slot index (positive = left
rotation, negative = right rotation).}
}
\value{
Integer; the automorphism group element
corresponding to that rotation.
}
\description{
Maps a CKKS slot index to the corresponding automorphism
index in the cyclotomic ring \code{Z[X]/(X^N + 1)}. The
automorphism group of the ring is isomorphic to the
multiplicative group \verb{(Z/2N)*}; this function returns the
representative of that group that corresponds to rotating
the plaintext slots by the given amount.
}
\details{
Used as a primitive by \code{\link[=find_automorphism_indices]{find_automorphism_indices()}} and
by code that needs to address automorphism keys directly
(for example, selectively generating eval keys for a
sparse set of rotation amounts).

\strong{R-first binding}:
\code{openfhe-python} does not bind \code{FindAutomorphismIndex}.
Logged in \code{notes/upstream-defects.md} under R-only
surface.
}
\seealso{
\code{\link[=find_automorphism_indices]{find_automorphism_indices()}} for the vector form.
}
