% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binfhe.R
\name{generate_lut_via_function}
\alias{generate_lut_via_function}
\title{Generate a lookup table for an arbitrary plaintext function}
\usage{
generate_lut_via_function(f, p)
}
\arguments{
\item{f}{A function \verb{function(m, p)} that returns the table
entry for input \code{m} under plaintext modulus \code{p}. Vectorised
functions are supported.}

\item{p}{The plaintext modulus (typically \code{get_max_plaintext_space(ctx)})}
}
\value{
A length-\code{p} numeric vector of LUT entries
}
\description{
Computes \code{f(0:(p-1), p)} and returns it as a numeric vector
suitable for \code{eval_func()}. This is the R-side analogue of
OpenFHE's \code{GenerateLUTviaFunction} — we don't bind the C++
helper because its signature takes a raw function pointer that
can't capture an R closure, and R is natively vectorised so a
pure-R helper is both simpler and faster than wiring an R
callback through cpp11.
}
