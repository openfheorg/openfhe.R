% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialization.R
\name{clear_eval_mult_keys}
\alias{clear_eval_mult_keys}
\title{Clear the EvalMult key cache}
\usage{
clear_eval_mult_keys(key_tag = NULL)
}
\arguments{
\item{key_tag}{\code{NULL} (default) to clear everything, or a
character scalar to clear only one tag's entries.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Clears the \code{CryptoContextImpl} internal EvalMult key map.
With \code{key_tag = NULL} (the default), clears the entire
cache — equivalent to the no-arg \code{ClearEvalMultKeys()} form
used by \code{\link[=clear_fhe_state]{clear_fhe_state()}}. With a non-NULL \code{key_tag},
clears only the entries registered under that tag,
preserving everything else. Useful in checkpoint workflows
where a single party's keys need to be evicted without
wiping the whole registry.
}
\seealso{
\code{\link[=clear_fhe_state]{clear_fhe_state()}}, \code{\link[=clear_eval_automorphism_keys]{clear_eval_automorphism_keys()}}
}
