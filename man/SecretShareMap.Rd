% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/secret-sharing.R
\name{SecretShareMap}
\alias{SecretShareMap}
\title{Map of secret-key shares for threshold-FHE abort recovery}
\usage{
SecretShareMap(ptr = NULL)
}
\arguments{
\item{ptr}{External pointer (internal use).}
}
\description{
Opaque S7 wrapper around a
\verb{shared_ptr<std::unordered_map<uint32_t, DCRTPoly>>}. Produced
by \code{\link[=share_keys]{share_keys()}} — each call returns one party's contribution
to the distributed shares of their own secret key. Consumed
by \code{\link[=recover_shared_key]{recover_shared_key()}}, which reconstructs the original
secret from \code{threshold} or more shares when a party drops out.
}
\details{
The map is keyed by party index (1-based uint32). Users do
not index into it directly; it is a transport format for the
secret-sharing protocol.
}
