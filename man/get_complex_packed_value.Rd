% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plaintext.R
\name{get_complex_packed_value}
\alias{get_complex_packed_value}
\title{Get complex values from a CKKS plaintext}
\usage{
get_complex_packed_value(pt)
}
\arguments{
\item{pt}{A \code{Plaintext}.}
}
\value{
A native R \code{complex} vector.
}
\description{
Reads the CKKS plaintext's internal slot vector as complex
numbers. Every CKKS plaintext slot carries a
\verb{std::complex<double>} internally; when a plaintext was
constructed from a real-valued vector via
\code{\link[=make_ckks_packed_plaintext]{make_ckks_packed_plaintext()}}, the imaginary parts are
all zero (up to CKKS encoding noise). When a plaintext
was constructed from a complex vector (the is-complex
dispatch path), both real and imaginary parts carry
information.
}
\seealso{
\code{\link[=get_real_packed_value]{get_real_packed_value()}} for the real-only view,
\code{\link[=make_ckks_packed_plaintext]{make_ckks_packed_plaintext()}} for the matching
constructor.
}
