% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plaintext-accessors.R
\name{plaintext_accessors}
\alias{plaintext_accessors}
\alias{get_noise_scale_deg}
\alias{get_length}
\alias{get_level}
\alias{get_scaling_factor}
\alias{get_log_precision}
\alias{get_formatted_values}
\alias{set_ckks_data_type}
\alias{get_encoding_type}
\alias{get_scaling_factor_int}
\alias{get_scheme_id}
\alias{is_encoded}
\alias{low_bound}
\alias{high_bound}
\alias{get_slots}
\alias{get_log_error}
\alias{get_coef_packed_value}
\alias{get_string_value}
\alias{get_element_ring_dimension}
\alias{set_scaling_factor}
\alias{set_scaling_factor_int}
\alias{set_noise_scale_deg}
\alias{set_level}
\alias{set_slots}
\alias{set_string_value}
\alias{set_int_vector_value}
\title{Plaintext accessors}
\usage{
get_noise_scale_deg(x, ...)

get_length(x, ...)

get_level(x, ...)

get_scaling_factor(x, ...)

get_log_precision(x, ...)

get_formatted_values(x, ...)

set_ckks_data_type(x, ...)

get_encoding_type(x, ...)

get_scaling_factor_int(x, ...)

get_scheme_id(x, ...)

is_encoded(x, ...)

low_bound(x, ...)

high_bound(x, ...)

get_slots(x, ...)

get_log_error(x, ...)

get_coef_packed_value(x, ...)

get_string_value(x, ...)

get_element_ring_dimension(x, ...)

set_scaling_factor(x, ...)

set_scaling_factor_int(x, ...)

set_noise_scale_deg(x, ...)

set_level(x, ...)

set_slots(x, ...)

set_string_value(x, ...)

set_int_vector_value(x, ...)
}
\arguments{
\item{x}{A \code{Plaintext}.}

\item{...}{Reserved for future method-specific arguments.
Setters accept a \code{value} argument here; \code{get_formatted_values}
accepts a \code{precision} integer.}
}
\value{
The underlying field value. Types vary per accessor.
}
\description{
Retrieve or set fields on a \code{Plaintext} object. Each accessor
wraps the corresponding upstream \verb{PlaintextImpl::Get*}/\verb{Set*}
method and returns its value unchanged.
}
\details{
Several base-class accessors are declared \code{virtual} and throw
\code{OPENFHE_THROW} in the base implementation (e.g. \code{get_string_value}
on a non-string plaintext, \code{get_log_precision} on a non-CKKS
plaintext). Calling an accessor on the wrong kind of plaintext
therefore raises an R error via \code{cli::cli_abort}, not a silent
wrong value.
}
\section{Functions}{
\itemize{
\item \code{get_noise_scale_deg}: Integer noise-scale-degree of a
CKKS plaintext. After construction the degree is the value
passed to \code{make_ckks_packed_plaintext(..., noise_scale_deg)}
under \code{FIXEDMANUAL} scaling; under \code{FLEXIBLEAUTO} the scheme
overrides the user-supplied value at context-generation
time. Incremented by each multiplication before a rescale.

\item \code{get_length}: Integer effective length of a
packed plaintext — the number of slots that hold user-supplied
values. Defaults to the full batch size at construction;
\code{set_length()} can shorten it for display or decryption
purposes.

\item \code{get_level}: Integer level of the plaintext
in the RNS modulus chain. \code{0} for a fresh plaintext;
incremented by each \code{rescale()} the plaintext survives. For
CKKS-packed plaintexts the level must match the ciphertext
level at every homomorphic operation, or the evaluator rejects
the pair.

\item \code{get_scaling_factor}: Numeric scaling factor for
CKKS-based plaintexts — the multiplier the real vector is
scaled by before encoding. Equals \code{2^scaling_mod_size} for a
freshly-encoded plaintext under FLEXIBLEAUTO; halves after
each rescale. For BFV/BGV plaintexts this is returned as a
default value (no rescaling applies).

\item \code{get_log_precision}: Numeric log2 of the precision
lost during CKKS encoding. Only meaningful for CKKS plaintexts;
throws via \code{OPENFHE_THROW} for BFV/BGV and is surfaced as an
R error by \code{cli::cli_abort}.

\item \code{get_formatted_values}: String-format the plaintext's
encoded values with \code{precision} decimal digits. Implemented
on the concrete plaintext subclass (packed, CKKS, string,
coefficient); throws on plaintexts whose subclass does not
override.

\item \code{set_ckks_data_type}: Set the CKKS data type to
\code{CKKSDataType$REAL} or \code{CKKSDataType$COMPLEX}. Only meaningful
for CKKS plaintexts. Most R vignettes use the default \code{REAL}
type; set to \code{COMPLEX} only when you are constructing a CKKS
plaintext from a complex vector.

\item \code{get_encoding_type}: integer
encoding type (see \code{PlaintextEncodings} for the enum values).

\item \code{get_scaling_factor_int}: BGV integer
scaling factor. Returned as a double carrying a losslessly
rounded 53-bit integer.

\item \code{get_scheme_id}: scheme
identifier (see \code{SchemeId} enum).

\item \code{is_encoded}: logical
"has the plaintext been encoded yet?" The factory methods
typically encode plaintexts eagerly, so this is \code{TRUE} for
fresh plaintexts. Returns \code{FALSE} only for plaintexts
constructed in a two-step uninitialised form.

\item \code{low_bound}: integer
lower bound that can be encoded with the current plaintext
modulus: \code{-floor(t / 2)}.

\item \code{high_bound}: integer
upper bound that can be encoded with the current plaintext
modulus: \code{floor(t / 2)}.

\item \code{get_slots}: integer CKKS
slot count. For \code{Plaintext} dispatch this is the \code{GetSlots()}
value set at construction.

\item \code{get_log_error}: numeric
log2 of the error estimate. Only meaningful for CKKS
plaintexts; throws for BFV/BGV.

\item \code{get_coef_packed_value}: integer
vector of the underlying coef-packed encoding. Throws for
plaintexts whose subclass is not coef-packed.

\item \code{get_string_value}: string value
of a string-encoded plaintext. Throws for plaintexts whose
subclass is not string-encoded.

\item \code{get_element_ring_dimension}: integer ring
dimension of the underlying \code{Element}. This is the plaintext's
view of the lattice ring dimension; typically matches the
crypto context's ring dimension.

\item \code{set_scaling_factor}: set the CKKS
plaintext scaling factor.

\item \code{set_scaling_factor_int}: set the BGV
plaintext integer scaling factor.

\item \code{set_noise_scale_deg}: set the
plaintext noise scale degree. Most users should not call this
directly — the factory methods and the evaluator manage
\code{noise_scale_deg} automatically.

\item \code{set_level}: set the
plaintext level. As with \code{set_noise_scale_deg}, most users
should not call this directly.

\item \code{set_slots}: set the CKKS
slot count. As with \code{set_length}, this is typically managed
by the factory methods.

\item \code{set_string_value}: set the
string value of a string-encoded plaintext. Throws for
plaintexts whose subclass is not string-encoded.

\item \code{set_int_vector_value}: set the
integer-vector value of an integer-encoded plaintext. Throws
for plaintexts whose subclass does not support an integer
vector.

}}
