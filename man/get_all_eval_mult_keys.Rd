% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval-key-map.R
\name{get_all_eval_mult_keys}
\alias{get_all_eval_mult_keys}
\title{Retrieve all registered EvalMult key vectors}
\usage{
get_all_eval_mult_keys()
}
\value{
A named list keyed by key-tag string. Each element
is a list of \code{EvalKey} objects.
}
\description{
Reads the entire \code{CryptoContextImpl} internal EvalMult key
map — a named R list keyed by secret-key tag, where each
element is itself a list of \code{EvalKey} objects (the vector
of multiplication-eval keys registered under that tag).
}
\details{
The returned list is a \strong{snapshot}: each \code{EvalKey} wraps
a fresh \code{shared_ptr} copy, so retained references survive
subsequent \code{\link[=clear_eval_mult_keys]{clear_eval_mult_keys()}} calls. The underlying
keys are still shared with the cc registry — modifications
through other paths remain visible.

Primary consumer: checkpoint/resume workflows that need to
audit which parties have keys registered before serializing
the cc.
}
\seealso{
\code{\link[=get_eval_mult_key_vector]{get_eval_mult_key_vector()}} for per-tag lookup,
\code{\link[=insert_eval_mult_key]{insert_eval_mult_key()}} for the write path.
}
