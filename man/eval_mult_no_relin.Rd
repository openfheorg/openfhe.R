% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-eval-9111.R
\name{eval_mult_no_relin}
\alias{eval_mult_no_relin}
\title{Homomorphic multiplication without relinearization}
\usage{
eval_mult_no_relin(x, ...)
}
\arguments{
\item{x}{A \code{Ciphertext}.}

\item{...}{Method-specific arguments: \code{y} — a \code{Ciphertext} to
multiply \code{x} by without relinearization.}
}
\value{
A \code{Ciphertext} at higher polynomial degree.
}
\description{
Returns the raw product of two ciphertexts as a higher-degree
ciphertext (the result has \code{n1 + n2 - 1} polynomial components
where the inputs had \code{n1} and \code{n2}). The standard \code{eval_mult()}
automatically relinearizes the result back to 2 components;
this variant skips the relinearization step so that multiple
multiplications can be chained at higher polynomial degree
before a single \code{relinearize()} call at the end. Used by the
\code{EvalMultAndRelinearize} fused variant and by
\code{EvalPolyWithPrecomp} for noise-optimal polynomial evaluation.
}
