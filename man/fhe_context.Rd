% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{fhe_context}
\alias{fhe_context}
\title{Create a fully homomorphic encryption context}
\usage{
fhe_context(scheme = c("BFV", "BGV", "CKKS"), ..., features = NULL)
}
\arguments{
\item{scheme}{Character: "BFV", "BGV", or "CKKS".}

\item{...}{Scheme-specific \code{CCParams} setter arguments. Forwarded
to \code{BFVParams()}, \code{BGVParams()}, or \code{CKKSParams()}. Example:
\code{fhe_context("BFV", plaintext_modulus = 65537, multiplicative_depth = 2)} or \code{fhe_context("CKKS", multiplicative_depth = 4, scaling_mod_size = 50, scaling_technique = ScalingTechnique$FLEXIBLEAUTO)}.}

\item{features}{Additional \code{Feature} values to enable on the
context beyond the default \code{PKE|KEYSWITCH|LEVELEDSHE} triple.}
}
\value{
A \code{CryptoContext} object.
}
\description{
High-level constructor that creates a \code{CryptoContext} with sensible
defaults. \code{PKE}, \code{KEYSWITCH}, and \code{LEVELEDSHE} features are enabled
automatically.
}
\details{
All scheme-specific \code{CCParams} setter arguments are accepted via
\code{...} and forwarded to the appropriate per-scheme constructor
(\code{BFVParams()}, \code{BGVParams()}, or \code{CKKSParams()}). See those
functions' argument lists for the valid per-scheme setter surface
— each scheme accepts only the setters that are \emph{not} disabled in
its upstream \verb{CCParams<T>} specialization (see discovery D013).
Passing an invalid scheme-specific argument produces an R-level
"unused argument" error at the underlying \verb{*Params()} call site.
}
\seealso{
\code{\link[=BFVParams]{BFVParams()}}, \code{\link[=BGVParams]{BGVParams()}}, \code{\link[=CKKSParams]{CKKSParams()}}
}
