% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binfhe.R
\name{eval_sign}
\alias{eval_sign}
\title{Evaluate sign on an encrypted value (functional bootstrapping)}
\usage{
eval_sign(ctx, ct, scheme_switch = FALSE)
}
\arguments{
\item{ctx}{A BinFHE context built with \code{arb_func = TRUE}}

\item{ct}{An LWECiphertext encrypted via \code{bin_encrypt(..., mod = Q)}}

\item{scheme_switch}{Logical; when \code{TRUE}, the output
ciphertext is encoded compatibly with the CKKS<->FHEW
scheme-switching pipeline (the \code{schemeSwitch} flag at
\code{binfhecontext.h} line 367). Default \code{FALSE} for the
standalone FHEW path. Per the upstream header
description, this is the "flag that indicates if it
should be compatible to scheme switching".}
}
\value{
An LWECiphertext encrypting 0 if the input was negative
(i.e. lay in the upper half of [0, Q)), 1 otherwise
}
\description{
Extracts the most-significant bit of an LWE ciphertext encrypted
under the large modulus Q. The context must have been created with
\code{arb_func = TRUE}.
}
