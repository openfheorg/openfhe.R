% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-eval.R
\name{eval_fast_rotation_ext}
\alias{eval_fast_rotation_ext}
\title{Extended hoisted slot rotation}
\usage{
eval_fast_rotation_ext(ct, ...)
}
\arguments{
\item{ct}{A \code{Ciphertext}.}

\item{...}{Method-specific arguments: \code{index} (rotation
amount, positive = left, negative = right), \code{precomp}
(a \code{FastRotationPrecomputation} from
\code{\link[=eval_fast_rotation_precompute]{eval_fast_rotation_precompute()}}), \code{add_first} (logical,
default \code{FALSE}).}
}
\value{
A \code{Ciphertext}.
}
\description{
Applies a rotation using precomputed digit decomposition
like \code{\link[=eval_fast_rotation]{eval_fast_rotation()}}, but with the extension that the
first digit of the decomposition can be folded into the
output before the rotation is applied (controlled by
\code{add_first}). Used inside the CKKS bootstrap fast-rotation
inner loop per \code{openfhe-development}'s
\code{scheme/base-scheme.cpp}. The eval-key map is pulled from
the \code{CryptoContext} internal registry via the ciphertext's
key tag, so there is no \code{EvalKeyMap} argument at the R
boundary — the automorphism keys must already be resident
on the cc (call \code{key_gen(cc, rotations = ...)} to populate
them, then reuse the \code{ct} here).
}
\seealso{
\code{\link[=eval_fast_rotation]{eval_fast_rotation()}}
}
