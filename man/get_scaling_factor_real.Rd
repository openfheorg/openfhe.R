% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ciphertext-accessors.R
\name{get_scaling_factor_real}
\alias{get_scaling_factor_real}
\title{Real-valued CKKS scaling factor at a modulus-chain level}
\usage{
get_scaling_factor_real(cc, ...)
}
\arguments{
\item{cc}{A \code{CryptoContext}.}

\item{...}{Method-specific arguments. The CryptoContext method
accepts \code{level} (integer level in the RNS modulus chain,
default \code{0L} — the top of the chain = the scaling factor at
fresh encryption time).}
}
\value{
Numeric scalar.
}
\description{
Returns \code{cryptoParams->GetScalingFactorReal(level)} — the
double-valued scaling factor at the given \code{level} of the CKKS
modulus chain. Meaningful only for CKKS contexts; BFV/BGV
contexts return the default field value (effectively \code{1.0}).
}
\details{
Used by \code{ckks_scaling_factor_bits()} (which takes \code{log2} of the
level-0 value to recover the bit size originally set via
\code{set_scaling_mod_size()}) and by the Stage 2 form of
\code{fhe_ckks_tolerance()}.
}
