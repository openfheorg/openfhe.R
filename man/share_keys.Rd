% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/secret-sharing.R
\name{share_keys}
\alias{share_keys}
\title{Distribute a secret key into shares}
\usage{
share_keys(cc, sk, n_parties, threshold, index, sharing_scheme = "additive")
}
\arguments{
\item{cc}{A \code{CryptoContext} with the \code{MULTIPARTY} feature.}

\item{sk}{The \code{PrivateKey} to share.}

\item{n_parties}{Integer; total number of parties.}

\item{threshold}{Integer; minimum number of shares needed to
reconstruct. For \code{"additive"} this must be \code{n_parties - 1};
for \code{"shamir"} it is typically \code{floor(n_parties/2) + 1}.}

\item{index}{Integer; the 1-based index of the party owning
\code{sk} (the "my share index").}

\item{sharing_scheme}{Character; either \code{"additive"} (default)
or \code{"shamir"}.}
}
\value{
A \code{SecretShareMap} suitable for passing to
\code{\link[=recover_shared_key]{recover_shared_key()}}.
}
\description{
Produces the set of shares that party \code{index} would
distribute to the other parties under the chosen
\code{sharing_scheme}. The returned \code{SecretShareMap} is opaque;
in a real deployment the shares would be serialized and
routed over the network to each receiving party, and the
receiving parties would store them for use in an abort
recovery.
}
\details{
Two sharing schemes are supported:
\itemize{
\item \code{"additive"} — N-1 threshold; every party must contribute
their share to reconstruct. Robust against corruption of
any single party's storage but not against any party
dropping out.
\item \code{"shamir"} — \code{floor(N/2) + 1} threshold; the secret can be
reconstructed from any majority subset of the distributed
shares. Robust against up to \code{floor((N-1)/2)} parties
dropping out.
}
}
\seealso{
\code{\link[=recover_shared_key]{recover_shared_key()}}
}
