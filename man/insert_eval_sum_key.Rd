% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval-key-map.R
\name{insert_eval_sum_key}
\alias{insert_eval_sum_key}
\title{Insert a joined sum-key map into the cc registry}
\usage{
insert_eval_sum_key(eval_key_map, key_tag = "")
}
\arguments{
\item{eval_key_map}{An \code{EvalKeyMap} from
\code{\link[=multi_add_eval_sum_keys]{multi_add_eval_sum_keys()}} or constructed through the
distributed key-gen flow.}

\item{key_tag}{Character; the tag to register the map under.
Default \code{""}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
After combining multi-party shares via
\code{\link[=multi_add_eval_sum_keys]{multi_add_eval_sum_keys()}}, the joined map has to be
inserted back into the cc's internal static registry before
\code{eval_sum()} can consume it. \code{insert_eval_sum_key()} routes
the map through \code{CryptoContextImpl::InsertEvalSumKey} (which
delegates internally to \code{InsertEvalAutomorphismKey} — the
same static storage is shared between the two surfaces).
}
