% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval-key-map.R
\name{eval_sum_key_gen}
\alias{eval_sum_key_gen}
\title{Generate sum keys for a secret key}
\usage{
eval_sum_key_gen(cc, sk)
}
\arguments{
\item{cc}{A \code{CryptoContext}.}

\item{sk}{A \code{PrivateKey} whose tag will be used to key the
generated sum-key map in the cc's internal registry.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Populates the \code{CryptoContext}'s internal sum-key registry
(keyed by the secret key's tag) so that \code{\link[=eval_sum]{eval_sum()}} and the
multi-party sum-key protocol can consume the generated
entries. Closes a long-standing gap: the underlying
\code{CryptoContext__EvalSumKeyGen} cpp11 binding has been present
since the early phases but had no standalone R wrapper —
users had to go through \code{\link[=key_gen]{key_gen()}}'s side-effects only.
The wrapper lands so that the multi-party sum-key flow
(which needs to call this on each party's secret share) has
a direct R-level entry point.
}
