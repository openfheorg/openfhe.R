% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval-key-map.R
\name{get_eval_sum_key_map}
\alias{get_eval_sum_key_map}
\title{Retrieve the sum-key map for a given key tag}
\usage{
get_eval_sum_key_map(key_tag)
}
\arguments{
\item{key_tag}{Character; the key tag used when the map was
originally generated. Typically the \code{get_key_tag()} of the
secret key that produced it.}
}
\value{
An \code{EvalKeyMap}.
}
\description{
Accessor for the cc-internal static map populated by
\code{eval_sum_key_gen()}. Used by the multi-party sum protocol to
pull the lead party's initial eval-sum map so other parties
can produce their shares.
}
\details{
The underlying C++ call returns a \verb{const std::map&}; the R
wrapper copies the map into a fresh \code{shared_ptr} to give the
returned \code{EvalKeyMap} owning semantics. The returned map is a
snapshot: subsequent modifications to the cc registry are not
reflected.
}
