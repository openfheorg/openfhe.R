% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-encrypt.R
\name{make_packed_plaintext}
\alias{make_packed_plaintext}
\title{Make a packed integer plaintext}
\usage{
make_packed_plaintext(cc, values, noise_scale_deg = 1L, level = 0L)
}
\arguments{
\item{cc}{A \code{CryptoContext}.}

\item{values}{An integer vector to pack. Length must not exceed
\code{batch_size} set at context creation.}

\item{noise_scale_deg}{Integer degree of the initial scaling
factor applied to the encoded plaintext. Defaults to \code{1L};
only meaningful under \code{FIXEDMANUAL} scaling (under
\code{FLEXIBLEAUTO} the scheme overrides this value — see discovery
D011). Every current vignette leaves it at the default.}

\item{level}{Integer target level in the RNS modulus chain.
Defaults to \code{0L}, meaning "fresh level, matching a
just-encrypted ciphertext". Set to match the level of a
ciphertext the plaintext will interact with if the ciphertext
has already been rescaled.}
}
\value{
A \code{Plaintext}.
}
\description{
Encode an integer vector as a BFV / BGV packed plaintext. The
result is an unencrypted \code{Plaintext} object that can then be
passed to \code{encrypt()}.
}
