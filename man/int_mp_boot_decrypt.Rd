% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interactive-bootstrap.R
\name{int_mp_boot_decrypt}
\alias{int_mp_boot_decrypt}
\title{Multi-party masked decryption for interactive bootstrap}
\usage{
int_mp_boot_decrypt(sk, ct, a)
}
\arguments{
\item{sk}{A \code{PrivateKey} (this party's share).}

\item{ct}{A \code{Ciphertext} to refresh.}

\item{a}{A \code{Ciphertext} holding the common random element
from \code{\link[=int_mp_boot_random_element_gen]{int_mp_boot_random_element_gen()}}.}
}
\value{
A list of two \code{Ciphertext} objects (the party's
shares pair).
}
\description{
Each party calls this with their own secret share, the
ciphertext being refreshed, and the common random element
from \code{\link[=int_mp_boot_random_element_gen]{int_mp_boot_random_element_gen()}}. Returns a list of
two \code{Ciphertext} objects — the party's masked-decryption
"shares pair". Each party's shares pair gets collected and
fed into \code{\link[=int_mp_boot_add]{int_mp_boot_add()}}.
}
