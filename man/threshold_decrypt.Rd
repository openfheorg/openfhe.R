% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiparty.R
\name{threshold_decrypt}
\alias{threshold_decrypt}
\title{Threshold decryption convenience: lead + main + fusion in one call}
\usage{
threshold_decrypt(cc, sks, ct)
}
\arguments{
\item{cc}{A CryptoContext}

\item{sks}{A list of PrivateKey objects, lead first}

\item{ct}{The Ciphertext to decrypt}
}
\value{
A Plaintext
}
\description{
Performs a full n-of-n threshold decryption of \code{ct} given the
ordered list of party secret keys. The first key in \code{sks} is used
for \code{\link[=multiparty_decrypt_lead]{multiparty_decrypt_lead()}}; the remaining keys for
\code{\link[=multiparty_decrypt_main]{multiparty_decrypt_main()}}; the resulting partials are then
fused with \code{\link[=multiparty_decrypt_fusion]{multiparty_decrypt_fusion()}}.
}
\details{
Use this when you have all secret keys in one place (testing,
simulation, single-process demos). In a real distributed
deployment each site holds only its own secret key and the
partials travel over the network — that flow uses the lead /
main / fusion functions directly.
}
