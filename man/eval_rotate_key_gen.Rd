% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval-key-map.R
\name{eval_rotate_key_gen}
\alias{eval_rotate_key_gen}
\title{Generate rotation keys for a secret key}
\usage{
eval_rotate_key_gen(cc, sk, index_list)
}
\arguments{
\item{cc}{A \code{CryptoContext}.}

\item{sk}{A \code{PrivateKey} whose tag will be used to key the
generated rotation keys in the cc's internal registry.}

\item{index_list}{Integer vector of rotation indices.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Standalone wrapper around the
\code{CryptoContext::EvalRotateKeyGen(privateKey, indexList)} C++
method. Populates the \code{CryptoContext}'s internal automorphism
key registry for the supplied rotation indices so that
\code{\link[=eval_rotate]{eval_rotate()}} can consume them.
}
\details{
\code{\link[=key_gen]{key_gen()}} folds this into its \code{rotations = ...} argument as
a convenience for fresh keypairs. The standalone wrapper is
the right entry point when the secret key already exists —
for example as the lead-party rotation-key generation step in
a multi-party rotation protocol, where subsequent parties
contribute via \code{\link[=multi_eval_at_index_key_gen]{multi_eval_at_index_key_gen()}}.
}
