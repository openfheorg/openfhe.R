% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-eval-9111.R
\name{level_reduce}
\alias{level_reduce}
\title{Reduce the modulus chain by multiple levels}
\usage{
level_reduce(x, ...)
}
\arguments{
\item{x}{A \code{Ciphertext}.}

\item{...}{Method-specific arguments: \code{eval_key} (an \code{EvalKey}
from \code{key_gen}), \code{levels} (integer, default \code{1L}).}
}
\value{
A \code{Ciphertext} at \code{x$level + levels}.
}
\description{
Drops \code{levels} levels from \code{x}'s modulus chain in a single
operation. Useful when \code{x} is at a deeper level than the
ciphertext it will interact with next; level-reducing brings
them onto the same rung of the chain.
}
\details{
An evaluation key is required — supply it from
\code{key_gen(cc, eval_mult = TRUE)}.
}
