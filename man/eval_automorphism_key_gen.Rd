% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval-key-map.R
\name{eval_automorphism_key_gen}
\alias{eval_automorphism_key_gen}
\title{Generate automorphism evaluation keys for a set of indices}
\usage{
eval_automorphism_key_gen(cc, sk, indices)
}
\arguments{
\item{cc}{A \code{CryptoContext}.}

\item{sk}{A \code{PrivateKey}.}

\item{indices}{Integer vector of automorphism indices
(not slot indices — use \code{\link[=find_automorphism_indices]{find_automorphism_indices()}} to
compute them from slot indices).}
}
\value{
An \code{EvalKeyMap} with one entry per input index.
}
\description{
Generates the eval-key map needed to apply
\code{\link[=eval_automorphism]{eval_automorphism()}} at the given set of slot indices.
The generated keys are both inserted into the
CryptoContext's internal eval-automorphism-key registry
(keyed by \code{sk}'s tag) \strong{and} returned as an \code{EvalKeyMap}
handle that the caller can pass directly to
\code{\link[=eval_automorphism]{eval_automorphism()}}.
}
\details{
On the C++ side this is equivalent to calling
\code{EvalAutomorphismKeyGen(sk, indices)} which internally
calls \code{CryptoContextImpl::InsertEvalAutomorphismKey} with
the generated map (cryptocontext.h line 2237). The dual
return / registry-insert pattern matches the openfhe-python
behavior at the equivalent entry point.

Companion to \code{eval_rotate_key_gen()} (reached via
\code{\link[=key_gen]{key_gen()}}'s \code{rotations} argument): both populate the
same cc-internal storage. The automorphism form gives raw
access to the automorphism group element (bypassing the
rotate-to-automorphism slot mapping that
\code{eval_rotate_key_gen} performs internally).
}
\seealso{
\code{\link[=eval_automorphism]{eval_automorphism()}},
\code{\link[=find_automorphism_indices]{find_automorphism_indices()}}
}
