% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialization.R
\name{insert_eval_mult_key}
\alias{insert_eval_mult_key}
\title{Insert an EvalMult key vector into the cc registry}
\usage{
insert_eval_mult_key(eval_keys, key_tag = "")
}
\arguments{
\item{eval_keys}{A list of \code{EvalKey} objects (from
\code{\link[=multi_key_switch_gen]{multi_key_switch_gen()}}, \code{\link[=multi_add_eval_mult_keys]{multi_add_eval_mult_keys()}}, or
from a deserialization).}

\item{key_tag}{Character; the tag to register the vector
under. Default \code{""} (auto-detect from the first eval key
in the vector).}
}
\value{
\code{NULL}, invisibly.
}
\description{
Adds a vector of \code{EvalKey} objects to the CryptoContext's
internal EvalMult-key map under \code{key_tag}. Silently replaces
any existing matching keys. If \code{key_tag} is the empty string
(\code{""}, the default), the tag is retrieved from the eval-key
vector itself (each \code{EvalKey} carries its own tag).
}
\details{
Used in checkpoint/resume workflows: after
\code{fhe_deserialize_eval_keys()} or \code{\link[=multi_add_eval_mult_keys]{multi_add_eval_mult_keys()}}
produces a combined eval-mult key vector, this function
registers it into the cc's internal storage so that
subsequent \code{eval_mult()} calls on ciphertexts encrypted
under the associated party's key can consume it.
}
\seealso{
\code{\link[=insert_eval_sum_key]{insert_eval_sum_key()}}, \code{\link[=insert_eval_automorphism_key]{insert_eval_automorphism_key()}}
}
