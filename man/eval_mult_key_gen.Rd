% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval-key-map.R
\name{eval_mult_key_gen}
\alias{eval_mult_key_gen}
\title{Generate relinearization (eval-mult) keys for a secret key}
\usage{
eval_mult_key_gen(cc, sk)
}
\arguments{
\item{cc}{A \code{CryptoContext}.}

\item{sk}{A \code{PrivateKey} whose tag will be used to key the
generated eval-mult key in the cc's internal registry.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Standalone wrapper around the
\code{CryptoContext::EvalMultKeyGen(privateKey)} C++ method.
Populates the \code{CryptoContext}'s internal eval-mult registry
(keyed by the secret key's tag) so that ciphertext ×
ciphertext multiplication can be relinearized.
}
\details{
\code{\link[=key_gen]{key_gen()}} folds this into its \code{eval_mult = TRUE} branch as
a convenience for fresh keypairs. The standalone wrapper is
the right entry point when the secret key already exists —
for example in any threshold or multi-party flow that holds
a secret-key share but did not generate it through
\code{key_gen()}.
}
