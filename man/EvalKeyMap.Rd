% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval-key-map.R
\name{EvalKeyMap}
\alias{EvalKeyMap}
\title{Map of homomorphic evaluation keys}
\usage{
EvalKeyMap(ptr = NULL)
}
\arguments{
\item{ptr}{External pointer (internal use).}
}
\description{
Opaque S7 wrapper around a
\verb{shared_ptr<std::map<uint32_t, EvalKey<DCRTPoly>>>}. Produced
by the \verb{multi_eval_*_key_gen()} family and by
\code{\link[=get_eval_sum_key_map]{get_eval_sum_key_map()}} / \code{\link[=get_eval_automorphism_key_map]{get_eval_automorphism_key_map()}};
consumed by \code{\link[=multi_add_eval_sum_keys]{multi_add_eval_sum_keys()}},
\code{\link[=multi_add_eval_automorphism_keys]{multi_add_eval_automorphism_keys()}}, \code{\link[=insert_eval_sum_key]{insert_eval_sum_key()}},
and \code{\link[=insert_eval_automorphism_key]{insert_eval_automorphism_key()}}. The map is keyed by a
rotation/automorphism index and carries one \code{EvalKey} per
index.
}
\details{
Users do not construct or index into an \code{EvalKeyMap}
directly — it is a transport format for the multi-party
eval-key protocols. In a single-user protocol the same data
is tracked inside the \code{CryptoContext}'s internal key
registry (populated by \code{EvalSumKeyGen()} /
\code{EvalRotateKeyGen()}) and is only exposed as an \code{EvalKeyMap}
when the distributed-party flow needs to exchange it.
}
