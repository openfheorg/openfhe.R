% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{BFVParams}
\alias{BFVParams}
\title{BFV Parameters}
\usage{
BFVParams(
  plaintext_modulus = NULL,
  multiplicative_depth = NULL,
  scaling_mod_size = NULL,
  batch_size = NULL,
  security_level = NULL,
  secret_key_dist = NULL,
  key_switch_technique = NULL,
  ring_dim = NULL,
  digit_size = NULL,
  num_large_digits = NULL,
  standard_deviation = NULL,
  multiparty_mode = NULL,
  threshold_num_of_parties = NULL,
  multiplication_technique = NULL,
  max_relin_sk_deg = NULL,
  pre_mode = NULL,
  eval_add_count = NULL,
  key_switch_count = NULL,
  encryption_technique = NULL
)
}
\arguments{
\item{plaintext_modulus}{Integer modulus \code{t} for the BFV plaintext
space. BFV plaintexts are elements of \code{Z_t[x]/Phi_m(x)} and every
homomorphic operation is performed modulo \code{t}. Must be a prime
(or a composite supporting NTT in the batching case) and must be
at least large enough that the end-to-end computation does not
wrap modulo \code{t}. Typical values: \code{65537} (the smallest
batching-friendly prime) for small-integer arithmetic, \code{786433}
or larger when the operands or intermediate sums are larger.
There is no scheme-level default — the user must supply one.}

\item{multiplicative_depth}{Integer depth of the multiplication
circuit the context must support. BFV and BGV grow ciphertext
noise with each multiplication, and the ring modulus \code{q} is sized
by OpenFHE to survive exactly \code{multiplicative_depth} successive
multiplications plus an \code{EvalAdd} chain between them. Set to the
exact depth of your circuit; setting it too small causes
correctness failure at decrypt, setting it too large inflates
ring dimension and slows every operation. Couples tightly to
\code{security_level} (together they pin \code{ring_dim}).}

\item{scaling_mod_size}{Integer bit-size of each intermediate
scaling modulus in the RNS decomposition of \code{q}. Default leaves
OpenFHE to choose from its internal tables (typically 60 bits).
Override only when you know your depth budget needs a tighter
value; the upstream simple-integers example uses the default.}

\item{batch_size}{Integer number of SIMD-batched plaintext slots.
Default is \code{ring_dim / 2} under the full-packing convention.
Set to a smaller power of two when your input vector is short
and rotation cost dominates the circuit (the inner-product
idiom). Must divide \code{ring_dim / 2}. Couples to \code{ring_dim}.}

\item{security_level}{One of \code{SecurityLevel$HEStd_128_classic}
(default when unset), \code{HEStd_192_classic}, \code{HEStd_256_classic},
and their \verb{_quantum} counterparts. Fixes the target hardness
assumption and, together with \code{multiplicative_depth}, determines
the minimum ring dimension via the upstream lattice-parameters
tables in \code{core/lattice/stdlatticeparms.h}.}

\item{secret_key_dist}{One of \code{SecretKeyDist$GAUSSIAN},
\code{UNIFORM_TERNARY} (default under classic lattice parameters), or
\code{SPARSE_TERNARY}. Controls the coefficient distribution of the
secret key. Change only for research scenarios that demand a
specific distribution; every production vignette uses the
default.}

\item{key_switch_technique}{One of \code{KeySwitchTechnique$BV} (default
for BFV) or \code{HYBRID}. \code{BV} produces smaller evaluation keys and
is the simpler option; \code{HYBRID} is relevant only when you are
also setting \code{num_large_digits} and benchmarking rotation cost.}

\item{ring_dim}{Integer power-of-two lattice ring dimension. Default
(\code{NULL}) asks OpenFHE to compute the minimum ring dimension that
satisfies \code{security_level} at the chosen \code{multiplicative_depth}.
Override only to force a larger value than the auto-selection,
typically to reserve headroom for later parameter sweeps.}

\item{digit_size}{Integer \code{r} for BV key-switching: the base-\code{2^r}
digit decomposition of the ciphertext during a key-switch.
Default \code{0L} selects the upstream default. Larger values
decrease the number of digit multiplications at the cost of
larger per-digit noise.}

\item{num_large_digits}{Integer number of digit groupings in
HYBRID key switching. Only meaningful when
\code{key_switch_technique = KeySwitchTechnique$HYBRID}; ignored
otherwise. Default \code{0L} lets OpenFHE pick.}

\item{standard_deviation}{Numeric standard deviation of the
Gaussian error distribution used during key generation and
encryption. Default (\code{NULL}) uses the upstream default
\code{3.19}. Override only in research scenarios.}

\item{multiparty_mode}{One of
\code{MultipartyMode$FIXED_NOISE_MULTIPARTY} or
\code{NOISE_FLOODING_MULTIPARTY}. Selects between threshold-FHE with
fixed noise (no flooding) and noise-flooding-protected
threshold-FHE. Both cox-threshold and CVXR-consensus-ADMM
vignettes rely on threshold paths; leave at upstream default
unless you are intentionally tuning the leakage/performance
trade-off.}

\item{threshold_num_of_parties}{Integer count of parties in an
n-of-n threshold protocol. Ignored in non-threshold contexts.
The cox-threshold vignette uses 2; the threshold-fhe-5p
Python example uses 5.}

\item{multiplication_technique}{One of
\code{MultiplicationTechnique$BEHZ}, \code{HPS}, \code{HPSPOVERQ},
\code{HPSPOVERQLEVELED}. BFV-specific choice of how the plaintext
modulus interacts with the ciphertext modulus during multiply.
Default upstream is \code{HPSPOVERQLEVELED}; override only if you
are benchmarking multiplication-path variants.}

\item{max_relin_sk_deg}{\verb{parity-deferred:} maximum degree of the
secret key that can be relinearized. Upstream default is 2.
No current vignette or Python example exercises this; the
cpp11 binding is in place so a later release can promote it
without a recompile.}

\item{pre_mode}{\verb{parity-deferred:} proxy re-encryption mode
(\code{PREMode$NOT_SET}, \code{INDCPA}, \code{FIXED_NOISE_HRA},
\code{NOISE_FLOODING_HRA}). Only meaningful if the \code{PRE} feature is
enabled on the context. No current vignette uses PRE.}

\item{eval_add_count}{\verb{parity-deferred:} upstream noise-budget
hint: maximum additions between multiplications. Used only by
the noise-flooding path. Default 0.}

\item{key_switch_count}{\verb{parity-deferred:} upstream noise-budget
hint: maximum key-switch count. Default 0.}

\item{encryption_technique}{\verb{parity-deferred:} BFV-specific
encryption variant (\code{EncryptionTechnique$STANDARD} or
\code{EXTENDED}). Default STANDARD.}
}
\value{
A \code{BFVParams} S7 object.
}
\description{
Constructor for the BFV scheme's \code{CCParams} surface. Every argument
maps 1:1 to an upstream \verb{CCParams<CryptoContextBFVRNS>::Set*} method
whose override is \emph{not} disabled in the BFV specialization. The 13
setters that BFV explicitly disables (\code{SetScalingTechnique},
\code{SetFirstModSize}, \code{SetPRENumHops}, \code{SetExecutionMode}, …) are not
exposed here; see discovery D013.
}
