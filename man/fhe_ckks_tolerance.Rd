% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tolerance.R
\name{fhe_ckks_tolerance}
\alias{fhe_ckks_tolerance}
\title{Per-test CKKS precision tolerance}
\usage{
fhe_ckks_tolerance(x, ...)
}
\arguments{
\item{x}{For the numeric method: integer multiplicative depth
at the point of decryption. For the Ciphertext method: a
\code{Ciphertext} object whose associated context supplies all
parameters.}

\item{...}{Method-specific arguments. The numeric method
accepts \code{scaling_factor_bits} (integer bit size of the
scaling modulus, typical values 50/59/78), \code{scaling_technique}
(a character string like \code{"FLEXIBLEAUTO"} or an integer from
the \code{ScalingTechnique} enum), and \code{k} (conservative
multiplicative factor, default 8). The Ciphertext method
accepts only \code{k}.}
}
\value{
Numeric scalar — the tolerance value to use as
\code{tolerance} in \code{tinytest::expect_equal()} or as \code{atol} in a
manual diff.
}
\description{
Computes a tolerance value suitable for comparing the cleartext
result of a CKKS circuit against a decrypted ciphertext. The
value is a function of the scheme's scaling factor, the
circuit's multiplicative depth at the point of decryption, and
the \code{ScalingTechnique}-specific precision loss per level.
}
\details{
Two dispatch forms:
\itemize{
\item \strong{Stage 1 (numeric)}: pass parameters as direct arguments.
Useful when the ciphertext isn't yet constructed — e.g. in a
fixture setup block that has to produce a tolerance before
calling \code{encrypt()}.
\item \strong{Stage 2 (Ciphertext)}: pass a \code{Ciphertext} directly. The
helper reads the associated \code{CryptoContext} via
\code{\link[=get_crypto_context]{get_crypto_context()}}, pulls the multiplicative depth (from
context) minus the ciphertext's current level, computes the
scaling factor bits via \code{\link[=ckks_scaling_factor_bits]{ckks_scaling_factor_bits()}}, and
looks up the scaling technique. This form is preferred at
test sites where a ciphertext exists.
}
}
\examples{
# Stage 1 — pass parameters directly:
tol1 <- fhe_ckks_tolerance(4L, 50L, "FLEXIBLEAUTO")

# Stage 2 — pass a ciphertext (requires a live CKKS context):
# cc <- fhe_context("CKKS", multiplicative_depth = 4L, scaling_mod_size = 50L)
# kp <- key_gen(cc, eval_mult = TRUE)
# pt <- make_ckks_packed_plaintext(cc, c(0.1, 0.2, 0.3, 0.4))
# ct <- encrypt(kp@public, pt, cc)
# tol2 <- fhe_ckks_tolerance(ct)
}
